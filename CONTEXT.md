# プロジェクトコンテキスト

このドキュメントは、Motion Tagger(Figmaプラグイン)の背景・設計判断とその理由をまとめたものです。使い方は [README.md](README.md) を参照してください。ここでは「なぜそうなっているか」に焦点を当てます。

## プロジェクトの目的

Figma上のパーツ(レイヤー)を指定して、フェードイン・フェードアウト・ムーブ・サイズ変更のアニメーションをduration/delay/easing付きで設定し、任意のIDとセットでJSONにエクスポートできるようにするプラグイン。加えて、設定したアニメーションをFigma純正のMotion機能(Beta)に反映して、実際の動きをFigma上で確認できるようにする。

## アーキテクチャ概要

- `src/code.ts`: Figmaプラグインのサンドボックス側。選択検知、`pluginData`の読み書き、JSON生成、Figma Motionへの反映を担当
- `src/ui.ts` + `src/ui.template.html`: プラグインUI(バニラTypeScript、フレームワーク不使用)。`ui.js`は`ui.template.html`にインライン埋め込みされ`dist/ui.html`として1ファイルにまとまる
- `src/types.ts`: UI⇔プラグイン間のメッセージ型、データ型の単一の情報源
- `src/easing.ts`: easingプリセット名とcubic-bezierの相互変換
- `src/move.ts`: ムーブアニメーションの座標解決ロジック(後述)
- `src/resize.ts`: サイズ変更アニメーションのモード判定ロジック(後述)
- `build.js`: esbuildでcode.ts / ui.tsをそれぞれバンドル

データの永続化はFigmaノードの`pluginData`(キー: `motionTag`)に対して行っており、Figmaファイル自体に保存される。

## 主要な設計判断とその理由

### 1. Figma Motion(Beta)を採用、Smart Animateは不採用

当初Smart Animate(プロトタイプのフレーム間遷移、フレームを複製してBefore/After間をつなぐ方式)で実装しかけたが、ユーザーの明示的な指示により、2026年Config 2026で発表されたFigma純正の「Motion」機能(1フレーム内でレイヤーを直接キーフレームアニメーションさせる、Plugin APIはBeta)に切り替えた。`node.applyManualKeyframeTrack()` / `node.removeManualKeyframeTrack()` / `node.setTimelineDuration()` などの`MotionNodeMixin` APIを使用(`@figma/plugin-typings` v1.131.0で型定義済み)。

- Motion APIは機能フラグ(`metronome`)による段階的提供のため、Figmaアカウント/ファイルによっては使えない場合がある。その場合、反映処理は静かに何もしない(エラー表示なし)設計にしている。プラグインの主要機能(タグ付け・JSON出力)はMotion連携が使えなくても問題なく動作する
- フレーム直下の最上位フレーム自体には直接キーフレームを設定できない(Motion APIの仕様)ため、その場合は自動的にスキップする

### 2. 自動同期・サイレント設計への変更

当初は「Figma Motionに反映して確認」ボタンを押した時だけMotionに反映する設計だったが、ユーザーからのフィードバックで以下のように変遷した。

1. 「+アニメーションを追加」を押した時点で自動的にMotion側にも反映されるようにしたい → `save-animation`/`delete-animation`/`delete-tag`の各メッセージハンドラから自動的に`applyMotionToFrame()`を呼ぶように変更
2. 自動反映が実現した結果、専用の確認ボタンは不要と判断 → ボタンとその一式(UI・メッセージ型・ハンドラ)を削除し、常時自動同期のみに一本化

この結果、Motion同期は完全にベストエフォート・サイレント設計になっている(通知・選択・ビューポート変更を一切行わず、失敗してもエラーを出さない)。プラグイン自体の主要機能(タグ・アニメーション設定の保存)は`pluginData`への書き込みが常に正しく行われることが保証されているため、この非対称設計が成立している。

### 3. IDの設計: 要素ID(一意) / アニメーションID(重複可)

- **要素(部品)ID**: 任意入力・空欄可。値を入力した場合のみドキュメント全体で一意性チェックを行う(空欄同士は重複扱いしない)
- **アニメーションID**: 要素IDとは別の、アニメーション単位の任意ラベル。**一意である必要はない** — 同じ性質のアニメーション(例: 複数要素の`fade_standard`というフェードイン)に共通のIDを付けられるようにするための仕様

「+アニメーションを追加」を押した時点でタグが無ければ、その時点の部品ID欄の値(空欄でも可)で自動的にタグを作成する。これにより、先に「部品IDを追加」ボタンを押さなくてもアニメーション設定に進める。

### 4. ID重複を検知した経緯と2つの原因

ユーザー環境で、同一の部品IDを持つ要素が異なるnodeIdで複数出現する不具合が報告された。調査の結果、2つの原因が判明した。

1. **Figma上での複製**: タグ付き済みのレイヤー/フレームを複製(Duplicate、ページ複製、ブランチのマージ等)すると`pluginData`がそのままコピーされ、プラグインのチェックを経由せずに重複IDが生まれる。これは自動修正せず、「全アニメ一覧」タブのJSONプレビュー上部に警告バナー(該当ID・レイヤー名・nodeId一覧)を表示するだけに留めている(ユーザーの意図しないデータ変更を避けるため)
2. **連続保存によるcheck-then-write競合状態(コード側のバグ、修正済み)**: ID重複チェック(`isIdAvailable`)は非同期処理のため、間を置かず連続で保存すると1回目の書き込み前に2回目のチェックが走り、両方とも「使用可能」と誤判定されることがあった。同期的なID予約(`reservedIds: Set<string>` / `tryClaimId()`)を追加してこの競合を解消した

### 5. ムーブアニメーションの座標指定方式

当初は「差分(dx/dy)」方式のみだった(現在位置からのオフセットで開始位置を指定)。ユーザーからの2段階のフィードバックで以下のように発展した。

1. デフォルトを絶対座標指定に変更し、差分方式はオプションとして選択できるようにしたい
2. 絶対座標モードはX/Yそれぞれについてfrom-to形式(開始位置と終了位置を両方明示)にしたい。fromの初期値は選択レイヤーの現在座標を使うこと

この過程で、旧実装のバグ(JSON出力側は`currentPos - dx`、Motion反映側は`dx`をそのまま使用しており符号が逆だった)を発見し、`src/move.ts`に座標解決ロジック(`getEffectiveMoveMode` / `resolveMovePositions`)を一本化することで修正した。

- **絶対座標モード(デフォルト)**: X/Yそれぞれについて開始(from)・終了(to)の絶対座標を個別入力。終了位置を実際のレイヤー位置と異なる場所にすることも可能。初期値は選択レイヤーの現在座標(from/toとも同じ値、つまり「移動なし」状態から開始)
- **差分モード**: 現在位置からのオフセット(dx/dy)で開始位置を指定。終了位置は常に現在のレイヤー位置
- `AnimationSpec.moveMode`が未設定の場合(モード切り替え機能導入前の既存データ)は、常に差分モード扱いにフォールバックする後方互換性を確保している

UIでは、from/toの入力欄の間に矢印(→)を配置し、下に「FROM」「TO」の小ラベルを添えることで、どちらが開始/終了かを視覚的に分かりやすくしている。

### 6. 「全アニメ一覧」タブ: カード一覧 → JSONプレビューへの置き換え

当初は「タグ付けされた要素一覧をカード形式で表示し、クリックでキャンバス上選択・個別に削除できる」UIだった。ユーザーから「JSONプレビューにしたい」との要望があり、**完全な置き換え**(カード一覧の削除)を選択した。これによりクリックでの選択・個別削除の機能は失われたが、ユーザーはこのトレードオフを理解した上で選択している。

JSONプレビューは`pluginData`の変更(追加・編集・削除)のたびに自動更新され、常に最新のエクスポート内容を反映する。「JSONをエクスポート」ボタンは、直近に受け取ったプレビューJSONをそのままダウンロードする(追加のプラグイン往復なし)。

**その後の追加(「アニメ一覧」タブ)**: JSONプレビューへの一本化後、ユーザーから改めて「今どういうアニメがあるかの一覧を、JSONプレビューとは別に見たい。レイヤーの一覧からドリルダウンしてそのレイヤーのアニメ一覧を見るイメージ」という要望があった。当初の一覧UIとの違いは、選択(canvas上での選択)や削除の機能は求められていない点 — あくまで**閲覧**目的。そのため完全新規のタブとして追加し(既存のJSONタブは変更せず並存)、`latestExportJson`(JSONタブと共有)から`animations.length > 0`の要素だけを抽出してレイヤー一覧を作り、クリックでそのレイヤーのアニメ一覧に遷移する、閲覧専用ドリルダウンとして実装した。当初のカード一覧が持っていた「クリックでキャンバス選択・個別削除」機能は復活させていない(`nodeId`がJSONに含まれない設計上、そもそも実装できない — 下記参照)。

### 7. `nodeId`をエクスポートJSONから除外

当初`ExportElement`に`nodeId`(Figma上のノードID)を含めていたが、「JSONに書かなくてもよい」との指示で除外した。ただし重複ID警告(UI内部でのみ使用)には引き続き必要なため、`findDuplicateIds()`はエクスポート後のJSONではなく、Figmaノードを直接走査して算出するように変更している。

### 8. サイズ変更アニメーション: ムーブと対になる2モード設計

ムーブ(絶対座標/差分の2モード)と同じ設計思想で、新しい`AnimationType`として`resize`を追加した。

- **パーセンテージモード(デフォルト)**: X方向・Y方向それぞれについて拡大率(%、100=等倍)をfrom-to形式で入力(初期値は`from: 80% → to: 100%`)。当初は単一値+終点は常に100%固定(ムーブの差分モードと同じ設計)だったが、ユーザーからの要望で終点(to)も明示的に指定できるよう変更した
- **絶対モード**: 幅・高さそれぞれについて開始(from)・終了(to)のピクセル寸法をfrom-to形式で個別入力。ムーブの絶対座標モードと対になる設計

ムーブとの実装上の違い: ムーブのTRANSLATION_X/Yは相対的なオフセット値のため、どちらのモードでも「現在位置からの差分」に変換する`resolveMovePositions()`が必要だった。一方サイズ変更では、Figma Motionの`SCALE_X`/`SCALE_Y`(倍率、1.0が等倍)と`WIDTH`/`HEIGHT`(絶対px)がそれぞれパーセンテージモード・絶対モードの値とほぼ1:1で対応するため、`src/resize.ts`はモード判定(`getEffectiveResizeMode`)のみを担い、座標変換ロジックは持たない(パーセンテージは`percent/100`として直接使用)。

**アンカーポイント(拡大縮小の基点)について**: プラグインからは制御できない。調査の結果、Figmaのアンカーポイントはネイティブの「Edit anchor point」(Option/Alt+R)でのみ設定可能な機能で、Motion Plugin APIの`KeyframePropertyFieldName`にはアンカー/ピボット関連のプロパティが存在しない([Figma公式ヘルプ](https://help.figma.com/hc/en-us/articles/41352588622615-Move-a-layer-s-anchor-point)、[フォーラムでの開発者からの回答](https://forum.figma.com/share-your-feedback-26/resize-origin-anchor-position-in-motion-55392)で確認)。また、`WIDTH`/`HEIGHT`を直接アニメーションさせる絶対モードは常に固定の基点(左上など)から伸縮し、アンカーポイントの影響を受けない。一方`SCALE_X`/`SCALE_Y`によるパーセンテージモードはアンカーポイント(デフォルトは中心)を基点に拡大縮小されるため、中心基点で拡縮させたい場合はパーセンテージモードを使い、Figma側でアンカーポイントをネイティブに調整するのが正攻法。

### 9. Figma Variablesとの連携: スコープと解決タイミング

「easingやdurationなどのプロパティに定義済みの変数を設定できないか」との要望を受け、3つの解釈(a. プリセットのコピー保存、b. 共有ライブラリの独自変数機構、c. Figma純正Variables)を`AskUserQuestion`で確認したところ、ユーザーは明確に「Figmaネイティブの Variables を使いたい」を選択した。

- **対象を duration / delay / easing の3プロパティに限定**: position/scale/sizeなど座標・サイズ系プロパティはVariablesバインディング対象に含めていない。要望が明示的に「easingやdurationなどのプロパティ」だったこと、および座標系は`move.ts`/`resize.ts`の from-to 変換ロジックと絡むため対象を広げると複雑化する判断から、スコープをタイミング・イージング系のみに絞った
- **型の対応**: Duration/DelayはFLOAT型変数、EasingはSTRING型変数のみを選択肢に表示する(`figma.variables.getLocalVariablesAsync("FLOAT"|"STRING")`)。STRING変数の値は`isValidEasingInput()`でプリセット名または`cubic-bezier(...)`形式かを検証し、不正な値ならリテラルのeasingへフォールバックする(変数側に想定外の文字列が入っていてもプラグインが壊れないようにするため)
- **解決タイミング: ライブ反応的ではなく、都度re-resolve**: Figma Variablesには「モードごとに異なる値」という仕組みがあり、同じ変数でもノードによって解決結果が変わり得る(`Variable.resolveForConsumer(node)`)。そのため、値をUI操作時に一度だけ解決してコピーするのではなく、`buildExportJson()`(JSON出力)と`applyMotionToFrame()`(Motion同期)の両方で、都度そのアニメーション対象ノードに対して解決し直す設計にした。これは既存の「常に再計算・再同期する」パターン(自動同期設計、moveの座標解決など)と一貫させるための選択であり、変数の値がFigma上で編集された瞬間にプラグインUIがリアルタイムに反応するわけではなく、次にアニメーションの追加・編集・削除操作が行われてre-syncが走った時点で反映される
- **リテラル値は常にフォールバックとして保持**: 変数を割り当てても`duration`/`delay`/`easing`のリテラル値はそのまま`AnimationSpec`に残し続ける。変数が後から削除された場合にも安全に動作させるため、また変数未割り当て時の表示値としても使うため
- **エクスポートJSONでの表現**: `duration`/`delay`/`easing`は常に解決済みの値を出力し(変数を意識せず消費できる)、変数が割り当てられている場合のみ`durationVariable`/`delayVariable`/`easingVariable`(変数名)を追加で出力する。これはトレーサビリティ目的で、値そのものは変数名を見なくても使える設計にしている
- **変数一覧の取得タイミングの不具合と対処**: 当初`sendVariables()`は`ui-ready`時(プラグインパネルを開いた瞬間)にのみ呼んでいた。ユーザーからの報告で、プラグインを開いたままFigma側でVariableを新規作成しても、プルダウンに反映されない不具合が発覚した(`ui-ready`は1回しか発火しないため)。`figma.on("selectionchange", ...)`のハンドラで`sendSelection()`と併せて`sendVariables()`も呼ぶように変更し、レイヤー選択のたびに変数一覧を再取得するようにして解消した。ドキュメント全体を監視する`documentchange`イベントは`dynamic-page`環境では`loadAllPagesAsync`の事前呼び出しが必要でコストが高いため採用していない
- **チームライブラリ変数への対応**: 当初はこのファイル自体のローカル変数のみが対象で、ユーザーから「将来的には他のライブラリファイルからも参照できるのが望ましい」との要望を受け、`figma.teamLibrary` APIで拡張した。ライブラリ変数はまだこのファイルに「インポート」されていない段階では通常の`Variable.id`を持たず、`LibraryVariable.key`という別形式の識別子しか持たない。そのため`durationVariableId`等のフィールドには、ローカル変数のid・ライブラリ変数のkeyのどちらも同じ文字列として保存できるようにし(型を分けるとUI・保存スキーマの両方が複雑化するため)、解決側の`resolveVariableRef()`が「まず`getVariableByIdAsync()`でローカルとして試す→ダメなら`importVariableByKeyAsync()`でライブラリkeyとしてインポートする」の順で試すフォールバック方式にした。インポートは副作用としてそのファイルのローカル変数になる(以後は`getVariableByIdAsync`で見つかる)が、Figmaの実装上インポート自体は冪等なので、都度re-resolveする既存の設計方針とも矛盾しない。なお、ライブラリを有効化する行為自体はPlugin APIからは行えない(Figma UIのAssetsパネルでユーザーが事前に有効化する必要がある)ため、その旨をREADMEに明記している

### 10. Motionタイムライン → プラグインの取り込み(片方向・ベストエフォート)

プラグイン経由でアニメを設定した後、Figma Motionのタイムラインパネルで直接キーフレームを編集すると、その変更はプラグインの`pluginData`には反映されない。既存の自動同期(`applyMotionToFrame`)は「プラグイン → Motion」の一方向のみで、しかもプラグイン側で何か1つでも編集すると、その時点でそのフレーム全体のMotionトラックが`pluginData`から再計算されて上書きされる設計だったため、ユーザーからは「タイムライン側の編集がいつの間にか消える/プラグインとタイムラインで内容が食い違う」という問題として報告された。要望は「これを解消するための同期ボタンがほしい」というものだった。

実装前にFigma Motion Plugin APIの型定義(`@figma/plugin-typings`)を直接確認し、逆方向の完全な同期が原理的に不可能なケースがあることを確認した:

- `node.manualKeyframeTracks`はプロパティ(`OPACITY`等)ごとに**1本のトラックしか持てない**構造(`Partial<Record<KeyframePropertyFieldName, ManualKeyframeBinding>>`)で、どのキーフレームが元々どの`AnimationSpec`(animId)から来たかという情報は一切保持されない。そのため、同じレイヤーに同種のアニメーション(例: fadeInが2つ)が複数ある場合、Motion側では既に1本のトラックに統合されてしまっており、どのキーフレームがどちらのアニメーションに対応するか判別不能
- このプラグインのデータモデルは常に「from→to」の2キーフレームのみを前提としている。タイムライン上でキーフレームを追加された場合、その情報を表現する手段がない
- `MotionEasing.type`には`CUSTOM_CUBIC_BEZIER`以外に`GENTLE`/`QUICK`/`BOUNCY`/`CUSTOM_SPRING`等のFigma独自のイージング(スプリング物理ベースなど)があり、このプラグインのeasingフィールド(プリセット名 or cubic-bezier文字列)には変換できない。またキーフレームのeasing自体が`VariableAlias`(Figma Variableへの直接バインド)になっているケースも同様に非対応
- moveの「差分(dx/dy)」モードは「終点=現在のレイヤー位置」という制約を前提にしており、タイムライン上で終点を自由な位置に動かされた結果を表現できない

これらを踏まえ、**「安全に判定できるケースだけ取り込み、それ以外は理由付きでスキップする」**という設計にした(黙って間違ったデータを書き込むより、取り込めないことを明示する方を優先)。判定は`readPropertyPair()`/`readAxisPair()`が`ReadPairResult | "removed" | "unsupported"`の3値を返す形にしており、"removed"(トラックが消えている→プラグイン側のアニメーションも削除)と"unsupported"(2キーフレームに収まらない・イージング非対応など→スキップして理由を報告)を明確に分けている。取り込みに成功したプロパティは、そのタイミングでVariableバインディングを解除する(前述のVariables機能との整合性 — バインディングを残すと次の自動同期でユーザーの手動編集が変数の値に上書きされてしまうため)。

もう一つの設計判断は、**取り込み後に`applyMotionToFrame()`を呼ばない**こと。仮に呼ぶと、スキップしたプロパティについては古い`pluginData`がそのままMotion側に押し戻され、まさに今回スキップしたはずのタイムライン編集をその場で上書きしてしまう。そのため「Motionタイムラインの変更を取り込む」は`pluginData`(とJSON出力・「アニメ一覧」タブ)を更新するだけの完全な片方向読み取りとし、Motion側の状態そのものには一切触れない。次にユーザーがプラグイン側で何か編集すれば、その時点で通常通り自動同期(プラグイン→Motion)が走る、という既存の仕組みは変えていない。

## 既知の制約

- Figma Motion API はBeta・機能フラグ提供のため、環境によっては利用できない(その場合サイレントに何もしない)
- 自動テストは無い(手動でのブラウザプレビュー確認とtsc型チェックのみ)
- `AnimationSpec`のデータ形式は`pluginData`にそのまま保存されるため、将来的にスキーマを変更する場合は`moveMode`のような後方互換フォールバックを検討する必要がある
- ID重複チェック・JSON出力(`collectTaggedNodes`)は現在ページのみが対象。ファイル全体を対象にするには`figma.loadAllPagesAsync()`が必要だが、ページ数の多いファイルで重くなり、しかも自動同期の設計上ほぼ全操作のたびに発生するコストだったため、現在ページのみに絞っている。複数ページにタグ付けする場合は、ページごとに個別にエクスポートする必要がある
- Duration/Delay/EasingのFigma Variablesバインディングは、チームライブラリの変数にも対応しているが、そのライブラリがFigma UI上で事前に有効化(Assetsパネル)されており、かつ変数がPublish済みである必要がある(Plugin APIからライブラリを有効化することはできない仕様のため)
- 「Motionタイムラインの変更を取り込む」は片方向・ベストエフォートの読み取りで、同種アニメーションの重複・3つ以上のキーフレーム・スプリング系イージング・イージングのVariableバインドなどは自動判定できず取り込まれない(理由付きでスキップ表示される)。詳細はREADMEの該当節を参照

## 開発コマンド

```bash
npm install
npm run build   # 1回ビルド
npm run watch   # 保存のたびに自動ビルド
npx tsc --noEmit  # 型チェックのみ
```

Figmaへの読み込み方はREADME.mdを参照。
