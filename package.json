{
  "name": "figma-motion-tagger",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin for tagging layers with IDs and exporting fade/move animation specs as JSON.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.94.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
